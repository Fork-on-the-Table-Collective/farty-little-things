extends Control


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	#implement a few secods of delay to show the game splash screen, then redirect to the vegan selector
	#create pulsing on "pushing..." text if it not possible by animation
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
